// CTA Band component

export function renderCtaBand(): string {
  return `
    <section class="cta-band">
      <div class="cta-inner">
        <h2 class="cta-title">Ready to elevate your event?</h2>
        <a href="#/quote" class="btn">Request a Quote</a>
      </div>
    </section>
  `
}
